[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "my-project"
version = "0.1.0"
description = "A minimal Python project scaffold"
requires-python = ">=3.8"
authors = ["Avintha Gupthar"]
